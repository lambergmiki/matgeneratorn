/**
 * Provisional function, subject for removal.
 * Adds two numbers together.
 *
 * @param {number} a First number to add
 * @param {number} b Second number to add
 * @returns {number} Sum of the two numbers
 */
export function sum (a, b) {
  return a + b
}
