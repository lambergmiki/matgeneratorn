services:
  matgeneratorn:
    build: .
    ports:
      - "5005:5005"
    environment:
      - PORT: 5005
      - BASE_URL: /matgeneratorn/
    restart: unless-stopped
    volumes:
      - .:/app
    command: npx nodemon server.js